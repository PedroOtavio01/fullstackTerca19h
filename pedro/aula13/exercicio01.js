const produtos = ["monitor", "teclado", "mouse", "gabinete", "mousepad", "headset", "placa", "memoria", "cooler", "processador"];

const exibirprodutos = (array) => {
    for (let i = 0; i < produtos.length; i++) {
    return (produtos[i]);
    }
}
console.log(produtos);

